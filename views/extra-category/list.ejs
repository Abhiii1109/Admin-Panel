<%- include('../layouts/header') %>

<div class="admin-layout">
  <%- include('../layouts/sidebar') %>
  
  <main class="main-content">
    <div class="card">
      <div class="card-header d-flex justify-between align-center">
        <h2 class="card-title">All Extra Categories</h2>
        <a href="/extra-category/create" class="btn btn-primary">
          <span>➕</span>
          <span>Add Extra Category</span>
        </a>
      </div>
      <div class="card-body">
        <% if (extraCategories.length > 0) { %>
          <div class="table-responsive">
            <table class="table">
              <thead>
                <tr>
                  <th>#</th>
                  <th>Name</th>
                  <th>Category</th>
                  <th>Subcategory</th>
                  <th>Created At</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <% extraCategories.forEach((extraCategory, index) => { %>
                  <tr>
                    <td><%= index + 1 %></td>
                    <td><%= extraCategory.name %></td>
                    <td>
                      <span class="badge badge-info">
                        <%= extraCategory.categoryId ? extraCategory.categoryId.name : 'N/A' %>
                      </span>
                    </td>
                    <td>
                      <span class="badge badge-success">
                        <%= extraCategory.subcategoryId ? extraCategory.subcategoryId.name : 'N/A' %>
                      </span>
                    </td>
                    <td><%= new Date(extraCategory.createdAt).toLocaleDateString() %></td>
                    <td>
                      <div class="d-flex gap-1">
                        <a href="/extra-category/edit/<%= extraCategory._id %>" class="btn btn-sm btn-secondary">Edit</a>
                        <form action="/extra-category/delete/<%= extraCategory._id %>" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this extra category?')">
                          <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                        </form>
                      </div>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } else { %>
          <p style="text-align: center; color: var(--color-gray-500); padding: 2rem;">
            No extra categories found. <a href="/extra-category/create" class="auth-link">Create your first extra category</a>
          </p>
        <% } %>
      </div>
    </div>
  </main>
</div>

<%- include('../layouts/footer') %>
